# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# env / secrets
.env
.env.local
*.key

# data: derived extracts are gitignored (>500KB pre-commit cap); generators are the source of record
data/raw/
data/exports/
*.parquet
data/seed_channels.csv

# notebooks
.ipynb_checkpoints/

# ml / mlflow
mlruns/
*.joblib
models/*.joblib

# dbt
warehouse/dbt_project/target/
warehouse/dbt_project/dbt_packages/
warehouse/dbt_project/logs/

# os / editor
.DS_Store
.vscode/
data/quota_usage.json
warehouse/dbt_project/.user.yml

mlflow.db
mlruns/
evaluation/cluster_assignments.csv
evaluation/niche_forecasts.csv

# Seed-expansion: raw dataset + local seed backup (regenerable via seed_from_dataset.py)
data/raw_seed/
data/seed_channels.bootstrap.csv
# Patch / build scratch
seedexp/
discoscale/
seed_builder/

# Streamlit secrets (PostHog key) — never commit
.streamlit/secrets.toml
analysis/output/
